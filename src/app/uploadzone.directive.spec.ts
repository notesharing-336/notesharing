import { UploadzoneDirective } from './uploadzone.directive';

describe('UploadzoneDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadzoneDirective();
    expect(directive).toBeTruthy();
  });
});
